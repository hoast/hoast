import * as _has from './has.js'
import * as _is from './is.js'
import * as _merge from './merge.js'

export const has = _has
export const is = _is
export const merge = _merge

export default {
  has: _has,
  is: _is,
  merge: _merge,
}
